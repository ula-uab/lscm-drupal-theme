name: Default
slots:
  groups:
    - type: component
      component: "italiagov:button_group"
      props:
        variant: default
        label: "First group"
      slots:
        buttons:
          - type: component
            component: "italiagov:button"
            slots:
              label: 1
            props:
              variant: primary
          - type: component
            component: "italiagov:button"
            slots:
              label: 2
            props:
              variant: primary
          - type: component
            component: "italiagov:button"
            slots:
              label: 3
            props:
              variant: primary
          - type: component
            component: "italiagov:button"
            slots:
              label: 4
            props:
              variant: primary
      attributes:
        class:
          - me-2
    - type: component
      component: "italiagov:button_group"
      props:
        variant: default
        label: "Second group"
      slots:
        buttons:
          - type: component
            component: "italiagov:button"
            slots:
              label: 5
            props:
              variant: secondary
          - type: component
            component: "italiagov:button"
            slots:
              label: 6
            props:
              variant: secondary
          - type: component
            component: "italiagov:button"
            slots:
              label: 7
            props:
              variant: secondary
      attributes:
        class:
          - me-2
    - type: component
      component: "italiagov:button_group"
      slots:
        buttons:
          - type: component
            component: "italiagov:button"
            slots:
              label: 8
            props:
              variant: info
      props:
        variant: default
        label: "Third group"
props:
  label: "Toolbar with button groups"
