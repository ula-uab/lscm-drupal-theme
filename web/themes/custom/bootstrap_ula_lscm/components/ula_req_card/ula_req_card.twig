{#
  ula_req_card — tarjeta de requisito de admisión.
  Props: icon, title, description. Markup y clases idénticos a la maqueta.
#}
<div class="req-card">
  <div class="req-card-icon">{{ icon }}</div>
  <h4>{{ title }}</h4>
  <p>{{ description }}</p>
</div>
