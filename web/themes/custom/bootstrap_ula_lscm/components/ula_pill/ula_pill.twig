{#
  ula_pill — pastilla / etiqueta individual.
  Prop: label. Markup y clase mínimos; el aspecto lo da ula_pill.css (chip por
  defecto). La variante del grupo (ula-pill-group--tag-card) puede reestilarla.
#}
<span class="ula-pill">{{ label }}</span>
